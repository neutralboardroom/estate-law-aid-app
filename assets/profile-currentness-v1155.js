(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.ELA_V1155_PROFILE_CURRENTNESS=api;if(root&&typeof root.dispatchEvent==='function'&&typeof root.CustomEvent==='function')root.dispatchEvent(new root.CustomEvent('ela:profile-currentness-ready'));})(typeof window!=='undefined'?window:globalThis,function(){'use strict';const KEY='ela:v1155:profile-currentness-review';const STATES=['unreviewed','source_checked','correction_needed','ready_for_visit','hold'];function safeParse(v,f){try{const x=JSON.parse(v);return x&&typeof x==='object'?x:f;}catch{return f;}}function read(storage){return safeParse(storage&&storage.getItem?storage.getItem(KEY):'',{});}function write(storage,records){if(storage&&storage.setItem)storage.setItem(KEY,JSON.stringify(records));return records;}function setStatus(storage,slug,status){if(!STATES.includes(status))throw new Error('invalid status');const records=read(storage);records[slug]={status,updatedAt:new Date().toISOString()};return write(storage,records);}function clear(storage){if(storage&&storage.removeItem)storage.removeItem(KEY);return {};}function summarize(records,queue){const out={total:queue.length,visible:queue.length,statuses:{}};for(const s of STATES)out.statuses[s]=0;for(const p of queue){const s=records[p.slug]?.status||'unreviewed';out.statuses[s]=(out.statuses[s]||0)+1;}return out;}function filter(queue,filters,records){const q=String(filters.query||'').trim().toLowerCase();return queue.filter(p=>(!filters.band||p.band===filters.band)&&(!filters.type||p.type===filters.type)&&(!filters.issue||p.issues.includes(filters.issue))&&(!filters.status||(records[p.slug]?.status||'unreviewed')===filters.status)&&(!q||[p.name,p.market,p.slug].join(' ').toLowerCase().includes(q)));}function exportPacket(queue,records){return {version:'1.1.55',exportedAt:new Date().toISOString(),storageMode:'browser-local-statuses-only',serverWriteEnabled:false,centralWriteBack:false,automaticPublication:false,automaticOutreach:false,sourceReviewIsNotCredentialVerification:true,records:queue.map(p=>({slug:p.slug,name:p.name,reviewDate:p.reviewDate,band:p.band,issues:p.issues,status:records[p.slug]?.status||'unreviewed'}))};}return Object.freeze({KEY,STATES,read,write,setStatus,clear,summarize,filter,exportPacket});});
