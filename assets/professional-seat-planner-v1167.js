(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.ELA_V1167_SEAT_PLANNER=api;if(root&&root.document)root.addEventListener('DOMContentLoaded',()=>api.bind(root.document));})(typeof window!=='undefined'?window:globalThis,function(){'use strict';const UNIT=1500;function integer(v){const n=Number(v);return Number.isInteger(n)&&n>=0&&n<=200?n:0}function calculate(input={}){const firmBase=Boolean(input.firmBase),seats=integer(input.seats),transfer=['none','individual-to-firm','firm-to-individual'].includes(input.transfer)?input.transfer:'none',timing=['next-cycle','same-day-credit'].includes(input.timing)?input.timing:'next-cycle';const base=firmBase?UNIT:0,seatTotal=seats*UNIT,total=base+seatTotal;const safeguards=['one active payer and one seat charge per covered attorney','no overlapping individual and firm seat charge','approved factual control survives membership or payer change','receipted effective date, proration or credit, support and rollback before live transfer'];return {ok:true,version:'1.1.67',currency:'USD',cadence:'month',firmBase,coveredAttorneySeats:seats,firmBaseCents:base,seatTotalCents:seatTotal,monthlyTotalCents:total,monthlyTotalUSD:(total/100).toFixed(2),transfer,timing,safeguards,liveCheckout:false,billing:false,entitlementMutation:false,truth:'Planning only. Smarter Justice remains the central billing and entitlement authority.'}}function bind(doc){const form=doc.getElementById('seatPlannerFormV1167');if(!form)return;const out=doc.getElementById('seatPlannerResultV1167');const render=()=>{const x=calculate({firmBase:form.elements.firmBase.checked,seats:form.elements.seats.value,transfer:form.elements.transfer.value,timing:form.elements.timing.value});out.innerHTML=`<h2>$${x.monthlyTotalUSD} USD per month</h2><p>${x.firmBase?'$15 firm base plus ':''}${x.coveredAttorneySeats} covered attorney seat${x.coveredAttorneySeats===1?'':'s'} at $15 each.</p><ul>${x.safeguards.map(s=>`<li>${s}</li>`).join('')}</ul><p class="notice"><strong>Not checkout:</strong> ${x.truth}</p>`};form.addEventListener('submit',e=>{e.preventDefault();render()});form.addEventListener('click',e=>{if(e.target&&e.target.dataset&&e.target.dataset.seatAction==='clear'){form.reset();form.elements.firmBase.checked=true;form.elements.seats.value='1';render()}});render()}return Object.freeze({UNIT,calculate,bind});});
