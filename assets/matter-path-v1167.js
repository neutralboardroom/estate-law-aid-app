(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.ELA_V1167_MATTER_PATH=api;if(typeof window!=='undefined')window.dispatchEvent(new Event('ela:matter-path-ready'));})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';const VERSION='1.1.67';
const ROUTES={planning:{label:'Estate planning',path:'/estate-planning/'},update:{label:'Update existing documents',path:'/update-existing-documents/'},probate:{label:'Probate and estate administration',path:'/probate/'},fiduciary:{label:'Executor, administrator, or trustee preparation',path:'/fiduciary-planning/'},guardianship:{label:'Guardianship preparation',path:'/guardianship/'},unsure:{label:'General estate starting path',path:'/start/'}};
function clean(v){return String(v??'').trim()}
function assess(input={}){const goal=ROUTES[input.goal]?input.goal:'unsure',route=ROUTES[goal],jurisdiction=clean(input.jurisdiction),urgency=clean(input.urgency),documents=clean(input.documents),unknowns=Math.max(0,Math.min(20,Number(input.unknowns)||0));let score=25;const reasons=[`You selected: ${route.label}.`],missing=[];if(['New York','New Jersey'].includes(jurisdiction)){score+=20;reasons.push(`${jurisdiction} is within the portal's current public starting scope.`)}else{missing.push('Confirm the correct state-specific law and official resources with a qualified professional.')}if(documents==='organized'){score+=25;reasons.push('You report that key document copies or names are organized.')}else if(documents==='some'){score+=12;missing.push('Gather the missing document copies, dates, and current versions.')}else missing.push('Start a document list; copies or file names are enough for preparation.');if(urgency==='yes'){reasons.push('You reported a court date, notice, deadline, recent death, capacity concern, or other urgency.');missing.push('Promptly confirm any date or required action with a qualified attorney or official source.')}else if(urgency==='unsure')missing.push('Check whether a court paper, notice, signing need, capacity concern, creditor issue, or deadline creates urgency.');else score+=15;if(unknowns===0)score+=15;else{score+=Math.max(0,15-unknowns*2);missing.push(`${unknowns} question${unknowns===1?'':'s'} or unknown${unknowns===1?'':'s'} remain to organize.`)}score=Math.max(0,Math.min(100,Math.round(score)));return {ok:true,version:VERSION,route,score,scoreMeaning:'Preparation completeness only — not legal readiness, validity, filing readiness, or a prediction.',reasons,missing,nextActions:[`Open ${route.label}.`,'Create a questions-and-unknowns list.','Gather copies or document names.','Use official sources or a qualified attorney to verify legal requirements.'],externalAI:false,legalAdvice:false,attorneyReviewed:false,automaticSave:false}}
function init(){const form=document.getElementById('matterPathFormV1167'),out=document.getElementById('matterPathOutputV1167');if(!form||!out)return false;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form),r=assess(Object.fromEntries(fd.entries()));out.innerHTML=`<div class="summary-box"><p class="eyebrow">Deterministic Matter Path</p><h2>${esc(r.route.label)}</h2><div class="summary-grid"><div class="summary-card"><strong>Preparation score</strong><br>${r.score}/100</div><div class="summary-card"><strong>Why this path</strong><br>${esc(r.reasons.join(' '))}</div></div><p class="notice"><strong>Meaning:</strong> ${esc(r.scoreMeaning)}</p><h3>Missing or uncertain items</h3><ul>${r.missing.length?r.missing.map(x=>`<li>${esc(x)}</li>`).join(''):'<li>No missing item was identified by this brief check. More facts may still matter.</li>'}</ul><h3>Suggested next actions</h3><ul>${r.nextActions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="page-actions"><a class="btn primary" href="${esc(r.route.path)}">Open suggested path</a><a class="btn light" href="/questions-and-unknowns/">Organize questions and unknowns</a></div><p class="small">Nothing was saved or sent. No external AI was used.</p></div>`;out.focus()});return true}
return {VERSION,ROUTES,assess,init};});
if(typeof window!=='undefined'){const start=()=>window.ELA_V1167_MATTER_PATH&&window.ELA_V1167_MATTER_PATH.init();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();}
